{
  "name": "personal-memory",
  "version": "0.4.0",
  "private": true,
  "description": "Local RAG personal memory store",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "memory": "./src/cli.ts"
  },
  "scripts": {
    "start": "tsx src/cli.ts ui",
    "memory": "tsx src/cli.ts",
    "index": "tsx src/cli.ts index",
    "test": "node --import tsx --test src/*.test.ts && bash scripts/migration-scripts.test.sh",
    "typecheck": "tsc --noEmit && tsc -p src/ui"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "@lancedb/lancedb": "^0.21.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
