---
description: Log an event/person memory into the local store
argument-hint: <what happened — who, what, when>
---

Use the `log-memory` skill (`skills/log-memory/SKILL.md`) to capture the
following into personal memory. Extract the structured fields (title, type, date,
people/team/tag slugs — reusing existing slugs), write a concrete body, store it
with `npx tsx src/cli.ts add`, then confirm the id, path, and a one-line recap.

What to remember:

$ARGUMENTS
