---
description: Recall & reason over personal memory to answer or plan
argument-hint: <question or planning topic>
---

Use the `recall-memory` skill (`skills/recall-memory/SKILL.md`) to answer the
following, grounded in stored personal memory. Choose adaptive depth: top-k
`memory query` for a narrow lookup, or filter-the-slice-then-read-everything
(`memory person` / `memory list` + reading the matched files in full, including
`memory/summaries/`) for synthesis/planning. Cite the entries you used.

Question / topic:

$ARGUMENTS
