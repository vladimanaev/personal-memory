---
description: Recall & reason over personal memory — answer, recap, or prep for a meeting
argument-hint: <question, recap, or planning topic>
---

Use the `recall-memory` skill (`skills/recall-memory/SKILL.md`) to answer the
following, grounded in stored personal memory. Choose adaptive depth: `memory
recall` with the user's question plus 1–3 of your own phrasings (keyword form,
entity form) for a narrow lookup, or filter-the-slice-then-read-everything
(`memory person` / `memory list` + reading the matched files in full, including
`memory/summaries/`) for synthesis, recaps, and meeting prep. Cite the entries
you used.

Question / topic:

$ARGUMENTS
